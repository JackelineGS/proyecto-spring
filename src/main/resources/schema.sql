-- Base de datos H2 (solo ejemplo de schema mínimo)
-- El esquema ahora es gestionado 100% por Hibernate (JPA) a través de las clases @Entity
-- Configurado con: spring.jpa.hibernate.ddl-auto=update

-- Instrucción inofensiva para evitar que Spring Boot colapse por archivo vacío
SELECT 1;
